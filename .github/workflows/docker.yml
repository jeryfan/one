name: 构建docker镜像
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: 开始构建
        run: docker build -t one-server:${date +%s} .
